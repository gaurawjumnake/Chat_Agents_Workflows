Review the existing AI workflow system in this repository and generate/update a `CLAUDE.md` file optimized for Claude Code workflows.

While generating the file:

- understand existing workflows, hooks, snippets, memory, specs, and instructions
- preserve existing workflow conventions
- avoid duplicating logic already present in repository docs
- reference reusable instructions/snippets/hooks instead of embedding large prompts
- optimize for long-context and autonomous Claude workflows
- keep instructions modular, structured, and practical
- prefer diff-based/context-based analysis over full repo scans
- enforce approval before destructive/refactor/deployment actions
- encourage use of specs, architecture docs, and existing memory/context before generating code
- support iterative reasoning and multi-step execution flows

Include guidance for:
- backend development
- frontend/UI development
- QA/testing
- DevOps workflows
- architecture review
- documentation updates
- incident/debug analysis

Include workflow orchestration guidance:
- when to load instructions
- when to use hooks
- when to update memory/context
- when to pause for approval
- how to handle large repositories incrementally

Include token optimization rules:
- use diffs first
- avoid repo-wide scans unless explicitly required
- load only impacted modules/files
- summarize context before deep analysis
- reuse memory/context references instead of repeated prompts

Include memory and context behavior:
- prefer markdown/Obsidian-based memory
- update memory incrementally
- avoid re-generating architecture context repeatedly

The generated `CLAUDE.md` should:
- act as the primary workflow guide for Claude Code
- support autonomous but controlled execution
- remain readable and maintainable
- avoid giant monolithic instructions
- reference repository workflow structure where appropriate

If an existing `CLAUDE.md` file exists:
- review it
- merge intelligently
- preserve useful project-specific rules
- remove duplicated or outdated instructions

Generate the final optimized `CLAUDE.md` file only.