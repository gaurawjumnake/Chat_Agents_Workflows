You are a senior backend engineer and AI workflow architect.

Your task is to UPDATE the existing AI-assisted development workflow in this repository so that it fully integrates the repository CONTEXT MEMORY system located under:

/ai-context/
/ai-memory/
/specs/
/snippets/

The workflow already supports:
- Spec-Driven Development (Spec-DD)
- Lightweight agents
- Prompt snippets
- Hooks
- Obsidian-based memory

Your responsibility is to MERGE the repository context system into the workflow so that:
- agents consume structured context files instead of raw repo scans
- token usage is reduced further
- architecture/design knowledge becomes persistent and reusable
- workflow remains incremental and maintainable

DO NOT redesign from scratch.
DO NOT duplicate existing workflow sections.

You must UPDATE and EXTEND the current workflow.

---

# 🎯 OBJECTIVES

1. Integrate /ai-context into all workflow phases
2. Make context files the PRIMARY source of repository knowledge
3. Prevent repeated repo exploration
4. Reduce unnecessary code scanning
5. Ensure context stays synchronized with implementation changes

---

# 📁 EXISTING CONTEXT SYSTEM

Repository contains:

/ai-context/
  architecture.md
  modules.md
  api-contracts.md
  data-models.md
  workflows.md
  dependencies.md
  patterns.md
  decisions.md

/ai-memory/
  /specs/
  /patterns/
  /bugs/
  /decisions/
  /snippets/

/specs/
  feature specs

/snippets/
  reusable prompt snippets

---

# 🧠 STEP 1 — UPDATE WORKFLOW PHASES

Modify EACH workflow phase so it explicitly uses context files.

For every phase define:

- Which context files should be loaded
- Which context files should NOT be loaded
- Maximum allowed context size
- Whether repo scanning is allowed

Example expectations:

## Requirement Phase
LOAD:
- architecture.md
- workflows.md

DO NOT LOAD:
- large implementation files

## Impact Analysis
LOAD:
- modules.md
- dependencies.md
- api-contracts.md

ONLY scan code if:
- context is missing
- files are outdated

## Implementation
LOAD:
- approved spec
- patterns.md
- relevant module summaries

DO NOT:
- rescan unrelated modules

---

# 🔁 STEP 2 — CONTEXT-FIRST EXECUTION MODEL

Update workflow rules so agents follow:

1. Context files FIRST
2. Specs SECOND
3. Source code THIRD

Agents must NEVER begin with large repo scans.

Define fallback rules:

IF context is incomplete:
- scan only targeted files
- summarize findings
- update context files

---

# ⚙️ STEP 3 — UPDATE AGENTS

Update all agents so they consume context intelligently.

For EACH agent define:

- Required context files
- Optional context files
- Context loading priority
- Context refresh conditions

Example:

## Design Agent
Required:
- architecture.md
- patterns.md
- workflows.md

Optional:
- decisions.md

Refresh context only if:
- architecture changed
- new module introduced

---

# 🧩 STEP 4 — CONTEXT SYNCHRONIZATION HOOKS

Create/update hooks that keep context synchronized.

Define hooks:

## Post-Implementation Hook
Update:
- modules.md
- workflows.md
- patterns.md (if reusable pattern introduced)

## Post-Refactor Hook
Update:
- architecture.md
- dependencies.md

## Post-Debug Hook
Update:
- decisions.md
- bugs memory

## Post-PR Hook
Verify:
- context consistency
- outdated sections
- orphaned module references

IMPORTANT:
Hooks must be SMALL and TARGETED.

Never regenerate all context files.

---

# 📜 STEP 5 — SPEC + CONTEXT LINKING

Update Spec-DD rules:

Each spec MUST reference:
- related modules
- workflows
- API contracts
- existing patterns

Add metadata section:

## Related Context
- modules:
- workflows:
- patterns:
- dependencies:

Implementation prompts must use:
SPEC + CONTEXT FILES
instead of raw requirements.

---

# 🧠 STEP 6 — CONTEXT REFRESH STRATEGY

Define rules for refreshing context.

DO NOT refresh automatically for every change.

Refresh ONLY when:
- architecture changes
- new module added
- API contract changes
- workflow changes
- dependency graph changes

Minor logic updates should NOT trigger full context regeneration.

---

# 🧮 STEP 7 — TOKEN OPTIMIZATION RULES

MANDATORY:

- Use summaries before source code
- Load only relevant context files
- Avoid repeated architecture explanations
- Prefer:
  - file references
  - module summaries
  - specs
instead of raw implementation

- NEVER:
  - scan whole repo repeatedly
  - reload all context files
  - duplicate context into prompts

---

# 🧱 STEP 8 — DAILY EXECUTION MODEL

Update workflow to define practical daily usage.

Example flow:

1. Load relevant spec
2. Load relevant context summaries
3. Run impact analysis
4. Implement targeted changes
5. Update affected context files only
6. Generate tests
7. Run review hooks

Workflow must be:
- IDE-friendly
- incremental
- low-token
- practical for large repositories

---

# 📂 STEP 9 — CONTEXT GOVERNANCE

Define maintenance rules:

- archive outdated context
- remove stale module references
- mark uncertain areas
- track undocumented modules

Add "Context Health Checklist":
- outdated files
- missing workflows
- undocumented APIs
- stale dependencies

---

# 🧾 STEP 10 — OUTPUT FORMAT

Generate:

1. Updated workflow sections
2. Updated agent definitions
3. Updated hook definitions
4. Context integration architecture
5. Daily execution examples
6. Context synchronization strategy
7. Token optimization improvements
8. Context governance checklist

DO NOT generate generic explanations.

