Review the existing AI workflow system in this repository and generate/update a `cursorrules.md` file optimized for Cursor IDE workflows.

While generating the file:

- understand existing workflows, hooks, snippets, memory, specs, and instructions
- preserve existing workflow conventions
- avoid duplicating logic already present in repository docs
- reference reusable instructions/snippets/hooks instead of embedding large prompts
- optimize for Cursor’s multi-file and agentic workflows
- keep instructions concise, modular, and practical
- prefer diff-based/context-based analysis over full repo scans
- enforce approval before destructive/refactor/deployment actions
- encourage use of specs and existing context before generating code
- support iterative implementation and repository-aware reasoning

Include guidance for:
- backend development
- frontend/UI development
- QA/testing
- DevOps workflows
- refactoring
- code reviews
- documentation updates

Include workflow execution guidance:
- when to load instructions
- when to use hooks
- when to update memory/context
- how to handle large repositories incrementally
- how to avoid unnecessary repo indexing/context loading

Include token optimization rules:
- avoid large prompts
- avoid repeated context
- load only impacted modules/files
- use diffs before opening entire files
- reuse existing architecture/context summaries

Include memory/context behavior:
- use markdown/Obsidian-based memory where available
- update memory incrementally
- avoid regenerating context files unnecessarily

The generated `cursorrules.md` should:
- act as persistent workflow guidance for Cursor
- remain readable and maintainable
- support scalable multi-file workflows
- avoid giant monolithic instructions
- reference repository workflow structure where appropriate

If an existing `cursorrules.md` file exists:
- review it
- merge intelligently
- preserve useful project-specific rules
- remove duplicated or outdated instructions

Generate the final optimized `cursorrules.md` file only.